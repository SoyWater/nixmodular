font_family JetBrainsMono Nerd Font
hide_window_decorations yes
window_padding_width 10 14

# Noctalia writes this file after each palette update. globinclude keeps the
# base config usable before the first generated palette exists.
globinclude /home/soywater/.local/state/noctalia/themes/kitty/*.conf
