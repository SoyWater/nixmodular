{ config, inputs, pkgs, defaultDesktopConfigHome, ... }:
let
  desktopConfigHome = config._module.args.desktopConfigHome or defaultDesktopConfigHome;
in
{
  wrapperVariants.kitty = {
    mirror = false;
    package = inputs.self.packages.${pkgs.stdenv.hostPlatform.system}.kitty.wrap {
      _module.args.desktopConfigHome = desktopConfigHome;
    };
  };
}
