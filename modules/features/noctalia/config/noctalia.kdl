window-rule {
    match app-id="dev.noctalia.Noctalia"
    open-floating true
    default-column-width { fixed 1080; }
    default-window-height { fixed 920; }
}

// Persistent wallpaper
layer-rule {
  match namespace="^noctalia-wallpaper"
  place-within-backdrop true
}
layout {
  background-color "transparent"
}

// Transparent bar
layer-rule {
  match namespace="^noctalia-bar-"
  background-effect {
    blur false
  }
}

debug {
  // Allows notification actions and window activation from Noctalia.
  honor-xdg-activation-with-invalid-serial
}

binds {
    // Noctalia panels and shell actions.
    Mod+S hotkey-overlay-title="Noctalia Control Center" { spawn-sh "exec \"$DESKTOP_SHELL\" msg panel-toggle control-center"; }
    Mod+Comma hotkey-overlay-title="Noctalia Settings" { spawn-sh "exec \"$DESKTOP_SHELL\" msg settings-toggle"; }
    Mod+Alt+L hotkey-overlay-title="Lock Screen" { spawn-sh "exec \"$DESKTOP_SHELL\" msg session lock"; }
    Mod+Y hotkey-overlay-title="Browse Wallpapers" { spawn-sh "exec \"$DESKTOP_SHELL\" msg panel-toggle wallpaper"; }
    Mod+Tab hotkey-overlay-title="Window Switcher" { spawn-sh "exec \"$DESKTOP_SHELL\" msg window-switcher"; }
    Mod+N hotkey-overlay-title="Notification Center" { spawn-sh "exec \"$DESKTOP_SHELL\" msg panel-toggle control-center notifications"; }

    XF86MonBrightnessDown allow-when-locked=true { spawn-sh "exec \"$DESKTOP_SHELL\" msg brightness-down"; }
    XF86MonBrightnessUp allow-when-locked=true { spawn-sh "exec \"$DESKTOP_SHELL\" msg brightness-up"; }

}

include optional=true "~/.local/state/noctalia/themes/niri/noctalia.kdl"
